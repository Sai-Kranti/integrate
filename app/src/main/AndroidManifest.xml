<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.monday">

    <application
        android:allowBackup="true"
        android:icon="@drawable/draw_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NotifActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".IdeateActivity" />
        <activity android:name=".InvestorActivity" />
        <activity android:name=".VeryFinalActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InfoActivity" />
        <activity android:name=".FTActivity" />
        <activity android:name=".TailorActivity"></activity>
    </application>

</manifest>